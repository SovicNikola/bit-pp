// 1 naredba deklaracije
var speed = 166;
// 2 naredba deklaracije
var speed_limit_down = 60;
//3 naredba deklaracije
var speed_limit_up = 120;
//4 if naredba
if (speed >= speed_limit_down && speed <= speed_limit_up) {
    // 5 .... uslov ispunjen.... 
    console.log("Perfect!")
}
else {
    // 6 ... uslov nijw ispunjen...

    console.log("speed down")
}
speed >= speed_limit_down && speed <= speed_limit_up
console.log(speed);

